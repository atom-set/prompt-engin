# Skills 管理工具使用指南

> **说明**：Skills 管理工具提供检验、优化、整合、优先级管理功能
> **创建时间**：2026-01-29

---

## 📖 简介

Skills 管理工具提供两种使用方式：

1. **简化版（推荐）** - `skill-engine report` 一键生成完整报告
2. **完整版** - `skill-engine manage` 提供详细的分析和管理功能

---

## 🚀 快速开始（推荐）

### 最简单的方式

```bash
# 一键生成完整分析报告（包含检验、优化建议、优先级分析等）
python3 -m skill_engine.cli report

# 报告会自动保存到 skills_report.txt
```

就这么简单！报告会包含：
- ✅ 所有 skills 的检验结果
- 💡 优化建议
- 🎯 优先级分析
- 🔗 整合建议

### 指定输出文件

```bash
# 指定输出文件名
python3 -m skill_engine.cli report -o my_report.txt

# JSON 格式输出（便于程序处理）
python3 -m skill_engine.cli report -o report.json --format json
```

---

## 📋 完整功能（高级用法）

如果需要更细粒度的控制，可以使用 `manage` 命令：

```bash
# 检验所有 skills
python3 -m skill_engine.cli manage validate

# 优化建议
python3 -m skill_engine.cli manage optimize

# 整合分析
python3 -m skill_engine.cli manage integrate

# 优先级分析
python3 -m skill_engine.cli manage priority

# 生成完整报告（包含所有分析）
python3 -m skill_engine.cli manage all
```

### 指定单个 Skill

```bash
# 检验单个 skill
python3 -m skill_engine.cli manage validate --skill core/tool-permission-system

# 优化单个 skill
python3 -m skill_engine.cli manage optimize --skill core/tool-permission-system

# 查看单个 skill 的优先级
python3 -m skill_engine.cli manage priority --skill core/tool-permission-system
```

### 输出到文件

```bash
# 输出文本格式报告
python3 -m skill_engine.cli manage all -o report.txt

# 输出 JSON 格式报告
python3 -m skill_engine.cli manage all -o report.json --format json
```

---

## 📋 功能详解

### 1. 检验（Validate）

检验功能检查 skills 的：

- ✅ **格式验证** - YAML frontmatter 格式、必需字段
- ✅ **内容质量** - 内容长度、描述质量、标签数量
- ✅ **完整性** - 必需章节（使用场景、触发条件、与其他规则的配合）
- ✅ **重复检测** - 检测相似或重复的内容

**示例输出**：

```
============================================================
🔍 Skills 检验报告
============================================================

📊 统计信息:
  - 总 skills 数: 31
  - 有问题的 skills: 10
  - 缺少章节的 skills: 0
  - 重复内容对: 0

⚠️  质量问题:
------------------------------------------------------------
  core/act-mode:
    - 缺少示例，建议添加使用示例
    - 缺少代码示例，建议添加代码示例
  ...
```

### 2. 优化（Optimize）

优化功能生成改进建议：

- 💡 **内容优化** - 补充缺失的示例、扩展描述
- 💡 **格式优化** - 标准化格式、优化标签
- 💡 **依赖关系** - 明确 skills 之间的依赖关系
- 💡 **优先级一致性** - 确保优先级设置合理

**示例输出**：

```
============================================================
✨ Skills 优化建议
============================================================

📌 core/act-mode:
  💡 添加代码示例或使用场景示例
  💡 明确依赖关系: mode-common, tool-permission-system
  ...
```

### 3. 整合（Integrate）

整合功能分析 skills 之间的关系：

- 🔄 **合并建议** - 识别相似或重复的 skills，建议合并
- 📁 **分组建议** - 根据标签或功能，建议创建子分类
- 🔗 **依赖分析** - 分析 skills 之间的依赖关系

**示例输出**：

```
============================================================
🔗 Skills 整合分析
============================================================

🔄 合并建议:
------------------------------------------------------------
  - skill1 ↔ skill2 (相似度: 85.2%)
    建议: 考虑合并 skill1 和 skill2（相似度 85.2%）

📁 分组建议:
------------------------------------------------------------
  - 标签 'code': 8 个 skills
    design-principles, organization, debugging, ...
```

### 4. 优先级（Priority）

优先级功能分析和管理 skills 的优先级：

- 🎯 **优先级分析** - 根据分类、标签、描述自动分析优先级
- 📊 **优先级报告** - 按优先级分组显示所有 skills
- 💡 **优先级建议** - 提供优先级设置建议

**优先级规则**：

- **优先级 1** - Core skills（核心规则，必须首先应用）
- **优先级 2** - Mode skills（模式相关，按条件应用）
- **优先级 3** - Code standards（代码规范，编写代码时应用）
- **优先级 4** - Domain skills（领域技能，按需加载）

**示例输出**：

```
============================================================
🎯 Skills 优先级分析
============================================================

优先级 1 (15 个 skills):
------------------------------------------------------------
  - core/tool-permission-system (core)
    工具权限系统，定义工具分类体系和统一检查流程...
  - core/mode-common (core)
    模式通用规则，包括模式切换、响应格式等...
  ...

优先级 2 (4 个 skills):
------------------------------------------------------------
  ...
```

---

## 📊 完整报告

使用 `manage all` 命令生成包含所有分析的完整报告：

```bash
python3 -m skill_engine.cli manage all -o full_report.txt
```

完整报告包含：

1. **检验报告** - 所有 skills 的检验结果
2. **优先级报告** - 按优先级分组的 skills
3. **整合报告** - 整合建议和机会
4. **优化建议** - 每个 skill 的优化建议

---

## 🔧 高级用法

### JSON 格式输出

生成 JSON 格式的报告，便于程序处理：

```bash
python3 -m skill_engine.cli manage all -o report.json --format json
```

JSON 报告包含：

- `summary` - 汇总统计信息
- `skills` - 所有 skills 的详细信息
- `duplicates` - 重复内容对
- `quality_issues` - 质量问题
- `optimization_suggestions` - 优化建议
- `priority_analysis` - 优先级分析
- `integration_opportunities` - 整合机会

### 结合其他命令

```bash
# 先检验，再优化
python3 -m skill_engine.cli manage validate
python3 -m skill_engine.cli manage optimize

# 查看优先级，然后生成完整报告
python3 -m skill_engine.cli manage priority
python3 -m skill_engine.cli manage all -o report.txt
```

---

## 💡 使用场景

### 场景 1：定期检查 Skills 质量

```bash
# 每周运行一次完整检查
python3 -m skill_engine.cli manage all -o weekly_report_$(date +%Y%m%d).txt
```

### 场景 2：添加新 Skill 后验证

```bash
# 创建新 skill 后
python3 -m skill_engine.cli create my-new-skill

# 验证新 skill
python3 -m skill_engine.cli manage validate --skill my-new-skill

# 查看优化建议
python3 -m skill_engine.cli manage optimize --skill my-new-skill
```

### 场景 3：重构 Skills 结构

```bash
# 分析整合机会
python3 -m skill_engine.cli manage integrate

# 查看优先级分布
python3 -m skill_engine.cli manage priority

# 生成完整分析报告
python3 -m skill_engine.cli manage all -o refactor_analysis.json --format json
```

### 场景 4：CI/CD 集成

```bash
# 在 CI 中验证所有 skills
python3 -m skill_engine.cli manage validate

# 如果有问题，退出码为 1
if [ $? -ne 0 ]; then
    echo "Skills 验证失败"
    exit 1
fi
```

---

## 📝 输出格式说明

### 文本格式（默认）

- 人类可读的格式
- 适合直接在终端查看
- 包含颜色和格式化

### JSON 格式

- 机器可读的格式
- 适合程序处理和分析
- 包含完整的结构化数据

---

## 🔗 相关文档

- [README.md](../README.md) - 项目主文档
- [skills/README.md](../skills/README.md) - Skills 库说明
- [QUICK_START.md](../QUICK_START.md) - 快速开始指南

---

## ❓ 常见问题

### Q: 如何修复检验发现的问题？

A: 根据检验报告中的建议，直接编辑对应的 skill 文件，然后重新运行检验。

### Q: 优先级是如何确定的？

A: 优先级根据以下规则确定：
1. 从描述中提取（如"优先级：1"）
2. 从标签中提取（如 `core` 标签 → 优先级 1）
3. 从分类中确定（如 `core` 分类 → 优先级 1）

### Q: 如何合并相似的 skills？

A: 使用整合分析找到相似的 skills，然后：
1. 手动合并内容
2. 删除重复的 skill
3. 更新依赖关系

### Q: JSON 报告如何使用？

A: JSON 报告可以用于：
- 自动化分析脚本
- 生成可视化图表
- 集成到其他工具

---

**如有问题，请参考项目文档或提交 Issue。**
